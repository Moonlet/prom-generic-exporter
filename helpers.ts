export const toBN = (data: string | number) => data;

export const fromAtomicAmount = (
  atomicAmount: string | number,
  decimals: number
) => atomicAmount;

export const toAtomicAmount = (amount: string | number, decimal: number) =>
  amount;
